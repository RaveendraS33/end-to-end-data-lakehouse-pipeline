# Normalize line endings to LF in the repository for all text files, so source
# renders consistently on GitHub and across contributor operating systems.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
